using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class failedatemts : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    /* RaycastHit hitInfo = new RaycastHit();
           Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
       //  Ray2D ray2d = Camera.main.ScreenPointToRay(Input.mousePosition);

         if(Physics.Raycast(ray, out hitInfo,100))
     {
                 //if (Input.GetMouseButtonDown(0))
                // {
                     Debug.Log("fadsdasdsaxz");
                // }*/
    // }


    // Update is called once per frame


    /*   RaycastHit2D hit;

       Ray ray = mainCamera.ScreenPointToRay(Input.mousePosition);
       if (hit = Physics2D.Raycast(ray.origin, new Vector2(0, 0)))
           Debug.Log(hit.collider.name);*/

//}

    /*  private void OnTriggerStay2D(Collider2D collision)
      {
          if (Input.GetMouseButtonDown(0) && collision.gameObject.CompareTag("note"))
          {
              Destroy(this.gameObject);
          }
      }

      private void OnCollisionStay2D(Collision2D collision)
      {
          if (Input.GetMouseButtonDown(0) && collision.gameObject.CompareTag("note"))
          {
              Destroy(this.gameObject);
          }
      }*/

    /*  private void OnMouseOver()
      {
          if (Input.GetMouseButtonDown(0))
          {
              Destroy(this.gameObject);
              Debug.Log("werkt");
          }
      }*/



//de if statement kun je in een if statement zetten waarbij je nog op mouse click checkt(of je maakt een gecombineerde if statement met &&)


//de bestaande if statement kun je in een tweede if statement zetten bedoel ik
}
